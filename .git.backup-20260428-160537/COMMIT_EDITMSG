security: Phase 3-6 — Python client, adapters, distribution hardening

- client.py: _resolve_binary validates path components, rejects .. traversal,
  verifies absolute paths exist, requires shutil.which match on PATH
- langchain_adapter.py: lazy ImportError on instantiation (not import time)
- autogen_adapter.py: lazy ImportError on instantiation
- crewai_adapter.py: explicit ImportError with install instructions
- download.sh: fetch checksums.txt from release, verify via sha256sum before install
- tests: +3 Python tests for path traversal, missing binary, absolute path

Co-authored-by: factory-droid[bot] <138933559+factory-droid[bot]@users.noreply.github.com>
